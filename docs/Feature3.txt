You are working in the RepSync repository, an offline-first Android workout app.

Read AGENTS.md first and follow it. Then inspect the relevant files before making changes. Reuse the existing architecture and UI patterns already present in the repo.

Implement the feature below end-to-end in code, not just as a plan.

Feature request:
Add support for non-weightlifting exercise tracking so users can log exercises like planks, wall sits, runs, walks, cycling, or cardio intervals with dedicated fields for time and optionally distance, instead of only weight and reps. The workout flow should support different exercise tracking types, including traditional weight/reps, duration-only, and duration-plus-distance.

User outcome:
Users can log both strength exercises and timed/cardio exercises in the same workout app using inputs that match the exercise type, such as seconds/minutes for a plank or time plus distance for a run.

Acceptance criteria:
- Users can create or edit exercises that use different tracking types.
- The app supports at least these exercise types:
  - weight + reps
  - duration only
  - duration + distance
- In active workouts, each exercise shows the correct input fields based on its tracking type.
- Plank-style exercises can be logged with time only.
- Running/cardio-style exercises can be logged with time and distance.
- Existing weightlifting exercises continue to work as they do today.
- Completed workout history stores and displays these new exercise values correctly.

Non-goals:
- Do not redesign the whole workout flow.
- Do not remove or break the current weight/reps tracking system.
- Do not add cloud sync, analytics, GPS tracking, or route maps.
- Do not add unnecessary new dependencies.

Constraints:
- Keep the app offline-first and local-only unless I explicitly ask otherwise.
- Keep the existing dark Material 3 visual style.
- Avoid unnecessary new dependencies.
- Prefer the repo's current patterns: Compose screens, StateFlow-backed ViewModels, Room/DataStore persistence, and Navigation Compose.

Likely files or areas to inspect first:
- app/src/main/java/com/repsync/app/ui/screens/NewWorkoutScreen.kt
- app/src/main/java/com/repsync/app/ui/screens/ActiveWorkoutScreen.kt
- app/src/main/java/com/repsync/app/ui/viewmodel/NewWorkoutViewModel.kt
- app/src/main/java/com/repsync/app/ui/viewmodel/ActiveWorkoutManager.kt
- app/src/main/java/com/repsync/app/data/entity/ExerciseEntity.kt
- app/src/main/java/com/repsync/app/data/entity/ExerciseSetEntity.kt
- app/src/main/java/com/repsync/app/data/entity/CompletedExerciseEntity.kt
- app/src/main/java/com/repsync/app/data/entity/CompletedSetEntity.kt
- app/src/main/java/com/repsync/app/data/dao/WorkoutDao.kt
- app/src/main/java/com/repsync/app/data/dao/CompletedWorkoutDao.kt
- app/src/main/java/com/repsync/app/data/RepSyncDatabase.kt

Data impact:
- Expected. This likely requires schema updates so exercise templates and completed workout records can store exercise tracking type and the new duration/distance values.
- If Room schema changes are needed, add a migration and preserve all existing user data.

Navigation impact:
- None expected unless inspection shows a supporting picker or editor flow needs a small route change.

Verification required:
- Run ./gradlew assembleDebug after the change if possible.
- Manually test these flows:
  - Create a normal strength exercise and confirm weight/reps still works.
  - Create a duration-only exercise like plank and confirm it shows time inputs in the workout flow.
  - Create a duration+distance exercise like running and confirm it shows time and distance inputs.
  - Save a workout template with mixed exercise types and reopen it for editing.
  - Start a workout from a template with mixed exercise types and complete it successfully.
  - Finish a workout and confirm completed history preserves the correct values.
  - Verify existing old workouts and templates still load after the migration.

Implementation rules:
1. Read the affected screen, ViewModel, DAO/entity, navigation, and theme files before editing.
2. Make the smallest coherent change that completely delivers the feature.
3. If Room schema changes are needed, add a migration and preserve user data.
4. If a new screen or route is needed, update both Screen.kt and RepSyncNavHost.kt.
5. Keep reusable UI in ui/components and screen-specific code in ui/screens.
6. State any assumptions you make, but do not stop for clarification unless the risk is high.
7. After coding, summarize what changed, which files were touched, what was verified, and any remaining risks.

Implementation notes:
- Add a clear exercise tracking type model, such as strength, duration, and duration_distance.
- Prefer simple, explicit data fields over overly abstract modeling.
- For duration, use a user-friendly format that supports minutes and seconds.
- For distance, use a consistent unit system in the UI and storage.
- Preserve backward compatibility for all existing strength exercises and historical data.
- Ensure previous-performance/history logic behaves sensibly for the new exercise types.

Deliverables:
- Implemented code changes
- Short summary
- Assumptions made
- Verification results
- Any follow-up suggestions only if they are truly important
